function InputPopup({ placeholderText, typeInput }) {
    return (
        <input type={ typeInput } placeholder={ placeholderText } className="mobile-navigation__input-registration"></input>
    );
}

export default InputPopup;
